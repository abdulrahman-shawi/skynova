import * as React from 'react';

interface ICustomrLayoutProps {
}

const CustomrLayout: React.FunctionComponent<ICustomrLayoutProps> = (props) => {
  return (
    <div className="">customer</div>
  );
};

export default CustomrLayout;
